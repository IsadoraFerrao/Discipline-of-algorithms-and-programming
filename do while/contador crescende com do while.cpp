#include <stdio.h>
int main() {
 int n;
 n = 1; //inicialização da variável
 while(n <= 5) { //teste
 printf("%d\n", n); //imprime a variável n
 n++; //++ é o operador de incremento. Equivalente: n = n + 1;
 }
}
